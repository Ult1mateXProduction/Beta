import axios from 'axios';
import authHeader from './auth-header';

const API_URL = 'https://ultimatex.tech:9000/api/';

class UserService {
  /*getSomething() {
    return axios.get(API_URL + 'some_method', { headers: authHeader() });
  }*/
}

export default new UserService();
